# --------------------------
# initialize tools
# --------------------------

# # Python pyenv
eval "$(pyenv init -)"

# # Ruby rvm
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

# # Javascript nvm
source /usr/share/nvm/init-nvm.sh

# replaced standard initialization with lazy-loader for performance issues
# source "${DOTFILES}/environment/lazy-load.sh/loader"

# project specific binaries need to be loaded first so the lookup is correct
PATH=./node_modules/.bin:$PATH      # prefer javascript npm binaries in local project
# PATH=./.composer/vendor/bin:$PATH     # Global Composer bin
# PATH=./.rvm/bin:$PATH                 # Add RVM to PATH for scripting. Make sure this is the last PATH variable change.
